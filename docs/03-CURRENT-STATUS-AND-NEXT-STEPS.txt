DENSO SENSE — CURRENT STATUS & NEXT STEPS
=========================================
Snapshot of where the project stands and what is still open.

DONE & ON main (https://github.com/j-jain/Landing-page)
-------------------------------------------------------
- Full Next.js 14 site matching Figma "Desktop - 4": all 8 sections, pixel-faithful on desktop.
- Animations: RTBS scroll-pinned pan (desktop) / swipe carousel (mobile); Use Cases &
  Testimonials hybrid auto+manual scroll rows; testimonial hover-video; Use Cases tap/click play.
- Responsive: desktop unchanged; mobile reflow (hero stacks, RTBS swipes, rows swipe, FAQ table
  scrolls). Mobile vertical+horizontal scrolling works over videos (touch-action pan-x pan-y).
- Book-a-Demo form -> POST /api/lead (validation; stub for real wiring).
- CI build check; footer year 2026; FAQ starts fully collapsed.
- Project relocated to C:\dev\denso-next (out of OneDrive) + junction at the old path.

DECISION (handoff): host EVERYTHING on Azure -> Azure Static Web Apps (HYBRID Next.js). SWA's
hybrid mode runs SSR + the /api/lead and /api/contact routes as-is (no porting to Functions).
App-size cap is 250 MB, so videos stay external (Azure Blob). Vercel is interim staging only.
See HANDOFF.md (dev team) and SHARE-WITH-AZURE-TEAM.md (owner) for the full handoff.

IN PROGRESS / WAITING
---------------------
- VERCEL (interim preview/staging): repo is ready to import. If already imported, it auto-deploys
  on each push to main. ACTION: import at vercel.com (Add New -> Project -> j-jain/Landing-page)
  if not done; then share the URL to verify the latest is live.
- AZURE BLOB VIDEOS: the 6 REAL DENSO Use-Case clips (~182 MB) now exist locally at
  public/videos/use-case-1..6.mp4 (gitignored). They resolve via NEXT_PUBLIC_MEDIA_BASE_URL in
  components/UseCases.tsx. ACTION: upload them to Azure Blob (Central India, behind Front Door) and
  set NEXT_PUBLIC_MEDIA_BASE_URL. (Testimonials use committed photos in public/assets/testi — no
  external host needed.)

OPEN TODOs (code)
-----------------
- app/api/lead/route.ts is a STUB: it validates + logs + returns ok. To finish for production:
    * Persist the lead (Azure Cosmos DB serverless or Table Storage, Central India).
    * Send email via Azure Communication Services (sales inbox + applicant).
    * Read secrets from env / Azure Key Vault.
- "I'm not a robot" is now REAL Google reCAPTCHA v2 (components/Recaptcha.tsx + lib/recaptcha.ts),
  wired into /api/lead. It just needs keys (see HANDOFF.md S6); with no keys it bypasses gracefully.
  (Front Door WAF can be added on top for extra protection if desired.)
- Use Cases now use the real DENSO footage (public/videos, served via Blob in prod). Testimonials
  use the real partner photos (public/assets/testi).
- "click here" link in the Diagnostics FAQ answer points to "#": wire to the real coverage page.

AZURE PRODUCTION ARCHITECTURE (proposed; DENSO mandate = all runtime on Azure)
------------------------------------------------------------------------------
- Hosting (CHOSEN): Azure Static Web Apps in HYBRID Next.js mode -> runs SSR + the /api/lead and
  /api/contact routes as-is (managed backend; NO static export, NO porting the API to a Function).
  250 MB app-size cap -> videos stay in Blob. (Azure App Service / Container Apps remain valid
  alternatives if SWA's hybrid preview ever becomes a constraint.)
- Edge/security: Azure Front Door (Premium for WAF) — CDN, TLS, routing, DDoS.
- Media: Azure Blob Storage (Hot) for videos/posters, served via Front Door.
- Data: Azure Cosmos DB (serverless) or Table Storage for leads.
- Email: Azure Communication Services (Email).
- Secrets: Azure Key Vault. Monitoring: Application Insights + Log Analytics.
- DNS: Azure DNS. Region: Central India (data residency for lead PII).
- CI/CD: GitHub Actions / Azure Pipelines -> the chosen Azure host.
- COMPLIANCE: Vercel is NON-Azure -> treat it as preview/staging only; production must be Azure.
  The Pexels videos are the one non-Azure runtime dependency and are resolved by the Blob migration
  (no exception needed once done).

HOW TO RUN / SHIP
-----------------
- Local dev:  cd C:\dev\denso-next  &&  npm run dev     (do NOT use the OneDrive path)
- Build:      npm run build         (then npm run start to serve the production build)
- Deploy:     push to main -> connected host auto-redeploys.

WHEN PICKING THIS BACK UP, START HERE
-------------------------------------
1. Get the Vercel preview URL up and confirm the latest commit is live.
2. Upload the 6 videos to Azure Blob, then set NEXT_PUBLIC_MEDIA_BASE_URL (GitHub repo secret,
   build-time) -> no code edit needed; UseCases.tsx already resolves <base>/<file>.
3. Create the Azure Static Web App (hybrid Next.js) on repo j-jain/Landing-page@main + Front Door;
   finish /api/lead + /api/contact (Cosmos/Table + ACS email). See HANDOFF.md for the full steps.
